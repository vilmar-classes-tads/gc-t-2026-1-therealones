Issue 0

Foi criada a classe servidor com campos opcionais e obrigatórios;
Foi criado o repositório do servidor reponsável por guardar os dados do servidor;
Foi feito a validação por CPF e email, dois usuários não podem ter o mesmo cpf ou o mesmo email;


Issue 1

Foi criada a classe Edital;
Foi feita a validação das datas do edital, o inicio não pode ser depois do fim;
É exibida uma Lista de editais na área administrativa.